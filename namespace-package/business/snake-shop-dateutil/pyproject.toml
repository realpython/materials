[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "snake-shop-dateutil"
version = "1.0.3"
description = "dateutils for the Snake Shop Corporation"
license = {text = "Snake Shop Software Licence Agreement"}

[tool.black]
line-length=79