[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "snake-corp-ordering"
version = "2.1.0"
description = "Ordering Package for the Snake Corporation"
license = { text = "Snake Corp Software Licence Agreement" }

[tool.black]
line-length = 79
